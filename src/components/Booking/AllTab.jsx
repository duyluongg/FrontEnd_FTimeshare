import React from 'react';

const AllTab = ({ bookingInfoConfirm, images, formatDate, handleCancelActive }) => {
    return (
        <>
            {/* JSX code for Wait to Confirm tab */}
        </>
    );
};

export default AllTab;